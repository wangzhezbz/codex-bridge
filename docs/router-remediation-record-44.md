# 整改记录 44：Router 开关响应优化与 Bridge 项目绑定更新

日期：2026-07-12

## Router 开关耗时根因

Router 健康确认或退出确认完成后，生命周期仍同步等待完整状态广播；Renderer 随后又执行无参数 `refresh()`，触发资源、会话和诊断详细扫描。扫描耗时被错误计入开关按钮的转圈时间。

## 修改

- Router 启动、停止仍保留配置事务、健康检查和进程退出确认。
- 完整状态广播改为后台执行，不阻塞已确认的启停结果。
- 开关按钮完成后只读取轻量核心状态，不再触发资源和会话详细扫描。
- 审计并替换 Embedded ZIP `20260712-143224`；SHA-256 为 `F6B9015B3C5358EEAA68A64284E009A58D27AA66865BF94D31C8B67E102E3FD7`，43 个文件，无禁止内容。
- 新增宿主 MCP 入口：将 Codex 进程提供的 `CODEX_THREAD_ID` 映射为 Bridge 所需的 `BRIDGE_CURRENT_CODEX_THREAD_ID`，再启动上游 MCP，实现真实任务作用域绑定。

## 验证

- Router 开关轻量刷新测试：修复前失败，修复后通过。
- Router 最终状态广播非阻塞测试：修复前失败，修复后通过。
- MCP 任务 ID 映射及配置入口测试通过。
- 新 Embedded 包文件逐项复制并校验，项目绑定缓存标识 `20260712-project-binding` 存在。
